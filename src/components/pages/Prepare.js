const Prepare = () => {
  // Map
  // Filter
  let a = [1, 2, 3, 4];

  a.map((item, index) => {
    console.log(item);
    console.log(index);
    console.log("----------");
  });
  return <div>Prepare</div>;
};

export default Prepare;
